import SwiftUI

enum SecuritySentryTheme {
    static let safe = Color(red: 0.20, green: 0.78, blue: 0.35)
    static let warning = Color(red: 1.00, green: 0.84, blue: 0.04)
    static let danger = Color(red: 1.00, green: 0.27, blue: 0.23)
    static let accent = Color(red: 0.39, green: 0.82, blue: 1.00)
    static let background = Color(red: 0.03, green: 0.06, blue: 0.10)
    static let panel = Color.white.opacity(0.06)
    static let border = Color.white.opacity(0.12)
    static let textPrimary = Color.white
    static let textSecondary = Color.white.opacity(0.72)
}
