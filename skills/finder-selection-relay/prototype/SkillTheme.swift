import SwiftUI

enum FinderSelectionRelayTheme {
    static let accent = Color(red: 0.039, green: 0.517, blue: 1.0)
    static let accentSoft = Color(red: 0.353, green: 0.784, blue: 0.980)
    static let background = Color(red: 0.050, green: 0.078, blue: 0.113)
    static let panel = Color.white.opacity(0.06)
    static let border = Color.white.opacity(0.12)
    static let textPrimary = Color.white
    static let textSecondary = Color.white.opacity(0.72)
}
